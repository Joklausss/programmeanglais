name = "lingualab-ia"
main = "worker.js"
compatibility_date = "2024-11-01"

# Origine autorisée à appeler la fonction (CORS).
# Remplacez par votre URL GitHub Pages, ou laissez "*" pour tester.
[vars]
ALLOWED_ORIGIN = "https://joklausss.github.io"

# La clé API n'est PAS ici : c'est un secret, ajouté par
#   npx wrangler secret put ANTHROPIC_API_KEY
